'use client';

/**
 * FloatingJarvis - Stub (original 3D component removed during cleanup)
 */

export default function FloatingJarvis(_props: { size?: string; isActive?: boolean }) {
  return null;
}
