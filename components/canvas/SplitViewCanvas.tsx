'use client';

/**
 * SplitViewCanvas - Stub (original removed during cleanup)
 */

interface SplitViewCanvasProps {
  volume?: string;
  filePath?: string;
  initialContent?: string;
}

export default function SplitViewCanvas({ volume, filePath }: SplitViewCanvasProps) {
  return (
    <div className="h-full flex items-center justify-center text-gray-500 text-sm">
      <p>Split view canvas not available</p>
    </div>
  );
}
