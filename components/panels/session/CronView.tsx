'use client';

/**
 * CronView - Stub (original removed during cleanup)
 */

export default function CronView() {
  return (
    <div className="h-full flex items-center justify-center text-gray-500 text-sm">
      <p>Cron scheduler not available</p>
    </div>
  );
}
